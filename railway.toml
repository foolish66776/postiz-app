[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.dev"

[deploy]
startCommand = "sh -c 'pnpm dlx prisma db push --schema ./libraries/nestjs-libraries/src/database/prisma/schema.prisma && pnpm dlx pm2 start --cwd .'"
restartPolicy = "ON_FAILURE"
restartPolicyMaxRetries = 10