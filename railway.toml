[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.dev"

[deploy]
startCommand = "sh -c 'pnpm run prisma-db-push && pnpm run --parallel pm2 && pm2 logs'"
restartPolicy = "ON_FAILURE"
restartPolicyMaxRetries = 10